# DNS module now outputs load balancer IP for manual DNS configuration
# The load balancer handles SSL termination and routing to Cloud Run
# No Cloud Run domain mapping is needed when using a load balancer

